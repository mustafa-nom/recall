# Copy to .env.local and adjust. Worker must be running (see repo README).

# Server-side proxy (Next.js Route Handlers → FastAPI worker)
WORKER_URL=http://127.0.0.1:8000

# Browser: Hub API, observer, WebSocket screenshot stream
NEXT_PUBLIC_WORKER_URL=http://127.0.0.1:8000
